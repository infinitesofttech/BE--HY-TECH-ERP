# Generated by Django 4.2.22 on 2026-08-05 05:58

from django.db import migrations, models
import django.db.models.deletion


def get_or_create_tax(Tax, rate):
    existing = Tax.objects.filter(rate=rate).first()
    if existing:
        return existing
    return Tax.objects.create(
        tax_id=f'TAX-{rate}',
        name=f'Tax {rate}%',
        rate=rate,
        status='active',
        applied_to='both',
    )


def backfill_estimation_tax(apps, schema_editor):
    Estimation = apps.get_model('estimations', 'Estimation')
    Tax = apps.get_model('finance', 'Tax')

    for row in Estimation.objects.all():
        rate = row.tax_decimal_tmp
        if rate:
            tax = get_or_create_tax(Tax, rate)
            Estimation.objects.filter(pk=row.pk).update(tax=tax)


class Migration(migrations.Migration):

    dependencies = [
        ('finance', '0004_seed_tax_from_taxrate'),
        ('estimations', '0001_initial'),
    ]

    operations = [
        migrations.RenameField(
            model_name='estimation',
            old_name='tax_percentage',
            new_name='tax_decimal_tmp',
        ),
        migrations.AddField(
            model_name='estimation',
            name='tax',
            field=models.ForeignKey(blank=True, null=True, on_delete=django.db.models.deletion.SET_NULL, related_name='estimations', to='finance.tax'),
        ),
        migrations.RunPython(backfill_estimation_tax),
        migrations.RemoveField(
            model_name='estimation',
            name='tax_decimal_tmp',
        ),
    ]
